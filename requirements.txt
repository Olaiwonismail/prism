cffi==2.0.0
numpy==2.4.6
pycparser==3.0
scipy==1.17.1
sounddevice==0.5.5
# Used only for the bundled rnnoise shared library (loaded via ctypes in
# prism/dsp/rnnoise_denoise.py); its Python wrapper and heavy deps are unused.
pyrnnoise==0.4.3
# Optional: only needed for the "deepfilternet" denoiser (prism/config.py).
# ~13 MB CPU wheel, no torch. Fetch the model with scripts/fetch_deepfilternet.py.
onnxruntime==1.26.0
