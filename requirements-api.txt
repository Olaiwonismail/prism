# Server deps for api.py (offline cleaning over HTTP).
# Headless: no sounddevice, no pyrnnoise (Windows-only native wheel).
# On a Linux server use a cross-platform denoiser (gtcrn / deepfilternet).
numpy==2.4.6
scipy==1.17.1
soundfile==0.14.0
onnxruntime==1.26.0      # for gtcrn / deepfilternet (CPU)
fastapi==0.137.1
uvicorn[standard]==0.49.0
python-multipart==0.0.32
